<video id="player" playsinline controls data-poster="<%= attached_present(lesson.thumbnail) ? url_for(lesson.thumbnail) : nil %>">
  <source src="<%= url_for(lesson.video) %>" type="video/mp4" />
  <source src="<%= url_for(lesson.video) %>" type="video/webm" />
</video>